import React from 'react';
// import PropTypes from 'prop-types';

const SidebarHeader = () => (
    <div className="SidebarHeader">
        <label className="SidebarHeader__Text">RumbleRocket</label>
    </div>
);

SidebarHeader.propTypes = {
    //
};

export default SidebarHeader;
